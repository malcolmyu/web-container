'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const headers = require('../headers-CFIXLFhf.cjs');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
const nodepodMatcher = headers.DEFAULT_SW_PATH;
async function buildResponse() {
  const { NextResponse } = await import('next/server');
  const body = await headers.readServiceWorkerSource((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('integrations/next.cjs', document.baseURI).href)));
  return new NextResponse(body, {
    status: 200,
    headers: headers.swResponseHeaders()
  });
}
async function GET() {
  return buildResponse();
}
async function nodepodProxy(req) {
  if (req.nextUrl.pathname !== headers.DEFAULT_SW_PATH) return null;
  return buildResponse();
}
const nodepodMiddleware = nodepodProxy;

exports.GET = GET;
exports.nodepodMatcher = nodepodMatcher;
exports.nodepodMiddleware = nodepodMiddleware;
exports.nodepodProxy = nodepodProxy;
//# sourceMappingURL=next.cjs.map
