'use strict';

Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: 'Module' } });

const headers = require('../headers-CFIXLFhf.cjs');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
function nodepod(opts = {}) {
  const swPath = opts.path ?? headers.DEFAULT_SW_PATH;
  const assetFileName = swPath.replace(/^\/+/, "");
  return {
    name: "nodepod",
    configureServer(server) {
      server.middlewares.use(async (req, res, next) => {
        const url = req.url?.split("?")[0];
        if (url !== swPath) return next();
        try {
          const source = await headers.readServiceWorkerSource((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('integrations/vite.cjs', document.baseURI).href)));
          const headers$1 = headers.swResponseHeaders();
          for (const [k, v] of Object.entries(headers$1)) res.setHeader(k, v);
          res.statusCode = 200;
          res.end(source);
        } catch (err) {
          res.statusCode = 500;
          res.setHeader("Content-Type", "text/plain");
          const msg = err instanceof Error ? err.message : String(err);
          res.end(`[nodepod/vite] failed to read __sw__.js: ${msg}`);
        }
      });
    },
    async generateBundle() {
      const source = await headers.readServiceWorkerSource((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('integrations/vite.cjs', document.baseURI).href)));
      this.emitFile({
        type: "asset",
        fileName: assetFileName,
        source
      });
    }
  };
}

exports.default = nodepod;
exports.nodepod = nodepod;
//# sourceMappingURL=vite.cjs.map
