'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const node_buffer = require('node:buffer');
const headers = require('../headers-CFIXLFhf.cjs');

var _documentCurrentScript = typeof document !== 'undefined' ? document.currentScript : null;
async function getServiceWorkerSource() {
  return headers.readServiceWorkerSource((typeof document === 'undefined' ? require('u' + 'rl').pathToFileURL(__filename).href : (_documentCurrentScript && _documentCurrentScript.tagName.toUpperCase() === 'SCRIPT' && _documentCurrentScript.src || new URL('integrations/server.cjs', document.baseURI).href)));
}
async function serveSW(_req) {
  const body = await getServiceWorkerSource();
  return new Response(body, {
    status: 200,
    headers: headers.swResponseHeaders()
  });
}
async function serveSWNode() {
  const source = await getServiceWorkerSource();
  const headers$1 = headers.swResponseHeaders();
  return {
    body: node_buffer.Buffer.from(source, "utf8"),
    headers: headers$1,
    contentType: headers$1["Content-Type"]
  };
}

exports.DEFAULT_SW_PATH = headers.DEFAULT_SW_PATH;
exports.getServiceWorkerSource = getServiceWorkerSource;
exports.serveSW = serveSW;
exports.serveSWNode = serveSWNode;
//# sourceMappingURL=server.cjs.map
