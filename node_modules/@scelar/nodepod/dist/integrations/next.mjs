import { D as DEFAULT_SW_PATH, r as readServiceWorkerSource, s as swResponseHeaders } from "../headers-DYciVWLo.js";
let GET, nodepodMatcher, nodepodMiddleware, nodepodProxy;
let __tla = (async () => {
  nodepodMatcher = DEFAULT_SW_PATH;
  async function buildResponse() {
    const { NextResponse } = await import("next/server").then(async (m) => {
      await m.__tla;
      return m;
    });
    const body = await readServiceWorkerSource(import.meta.url);
    return new NextResponse(body, {
      status: 200,
      headers: swResponseHeaders()
    });
  }
  GET = async function() {
    return buildResponse();
  };
  nodepodProxy = async function(req) {
    if (req.nextUrl.pathname !== DEFAULT_SW_PATH) return null;
    return buildResponse();
  };
  nodepodMiddleware = nodepodProxy;
})();
export {
  GET,
  __tla,
  nodepodMatcher,
  nodepodMiddleware,
  nodepodProxy
};
